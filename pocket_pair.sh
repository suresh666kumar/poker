grep shows poker_now_log_*.csv | grep "shows a A" | grep ", A"
grep shows poker_now_log_*.csv | grep "shows a K" | grep ", K"
grep shows poker_now_log_*.csv | grep "shows a Q" | grep ", Q"
grep shows poker_now_log_*.csv | grep "shows a J" | grep ", J"
grep shows poker_now_log_*.csv | grep "shows a 10" | grep ", 10"
grep shows poker_now_log_*.csv | grep "shows a 9" | grep ", 9"
grep shows poker_now_log_*.csv | grep "shows a 8" | grep ", 8"
grep shows poker_now_log_*.csv | grep "shows a 7" | grep ", 7"
grep shows poker_now_log_*.csv | grep "shows a 6" | grep ", 6"
grep shows poker_now_log_*.csv | grep "shows a 5" | grep ", 5"
grep shows poker_now_log_*.csv | grep "shows a 4" | grep ", 4"
grep shows poker_now_log_*.csv | grep "shows a 3" | grep ", 3"
grep shows poker_now_log_*.csv | grep "shows a 2" | grep ", 2"
